export type wakeUpTimeType = {
  id: number;
  hrs: string;
  min: string;
  period: string;
};

export type timeBoxType = {
  topTwo?: boolean;
  suggested?: boolean;
  hrs: string;
  min: string;
  period: string;
};
